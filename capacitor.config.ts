import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pistinadellloc.mantenimiento',
  appName: 'Pistina',
  webDir: 'out'
};

export default config;
